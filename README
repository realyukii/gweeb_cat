a x86_64 program for reading file contents written in two version:
- with the help of libc
- nolibc

features:
1. error checking, protect the program from crash unexpectedly:
* check if specified file exists

development:
compiled with fasm with intel syntax
