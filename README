a x86_64 program for reading file contents written in two version:
- with the help of libc
- nolibc

usage:
./cat [files]

features:
0. print simple usage string
1. error checking, protect the program by prevent it from crash unexpectedly:
* check if specified file exists
2. read from stdin if no cmdline arguments supplied 
3. concatenate multiple files

development:
compiled by GNU Assembler (GAS) and GCC with AT&T syntax

